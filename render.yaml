services:
  - type: web
    name: comparafci
    env: python
    plan: starter
    buildCommand: "./build.sh"
    startCommand: "gunicorn comparafci.wsgi:application --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.5